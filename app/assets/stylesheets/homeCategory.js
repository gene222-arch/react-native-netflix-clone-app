import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    categoryContainer: {
        marginBottom: 20
    },
    image: {
        width: 100,
        height: 170,
        resizeMode: 'cover',
        borderRadius: 5,
        marginRight: 5
    },
});

export default styles;